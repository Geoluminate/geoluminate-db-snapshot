============
Installation
============

At the command line::

    $ easy_install geoluminate-db-snapshot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv geoluminate-db-snapshot
    $ pip install geoluminate-db-snapshot
